active: true
derived: false
header: 'Deterministic execution of inferred setup steps'
level: '1.14'
links: []
normative: true
ref: ''
reviewed: null
text: |
  the solution SHALL execute the inferred setup steps inside the LXD container in a
  deterministic order before any atomic test is executed.
evaluation:
  type: deterministic
  method: integration_test
  success_criteria: |
    the inferred setup steps execute in the documented order and the container
    reaches a ready state for assessment
  failure_mode: abort remaining setup steps and log the failing command
  guardrails:
    - setup phase timeout 600s
    - each step exit code captured
    - dependency installation ordered before compilation steps
  human_escalation: setup phase exceeds 600s or a step returns non-zero exit code
