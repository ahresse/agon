active: true
derived: false
header: 'Bypass of automatic setup phase via CLI flag'
level: '1.16'
links: []
normative: true
ref: ''
reviewed: null
text: |
  the solution SHALL allow users to bypass the automatic setup phase via a CLI flag
  so that atomic tests can be run against a raw source tree.
evaluation:
  type: deterministic
  method: integration_test
  success_criteria: |
    when the bypass flag is present, no setup step is executed and atomic tests
    run immediately after extraction
  failure_mode: ignore the flag and execute the setup phase normally
  guardrails:
    - bypass flag validated as a boolean toggle
    - bypass flag documented in CLI help text
  human_escalation: never
