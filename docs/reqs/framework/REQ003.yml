active: true
derived: false
header: 'Atomic test definition with mandatory attributes'
level: 1.3
links: []
normative: true
ref: ''
reviewed: null
text: |
  the solution SHALL allow users to define atomic tests, each specifying a unique
  identifier, a human-readable name, a test type, a target path within the
  extracted source tree, an execution strategy, and a weight.
evaluation:
  type: deterministic
  method: unit_test
  success_criteria: |
    an atomic test definition containing all mandatory attributes is accepted;
    a definition missing any mandatory attribute is rejected with a structured
    error indicating the missing field
  failure_mode: reject the malformed atomic test definition and emit structured error log
  guardrails: attribute names validated against a published schema
  human_escalation: never
