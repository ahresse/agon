active: true
derived: false
header: 'Structured summary of assessment results'
level: 1.8
links: []
normative: true
ref: ''
reviewed: null
text: |
  the solution SHALL generate a structured summary containing, for each executed
  atomic test, its identifier, name, grade, weight, exit code, AI reasoning,
  and the final weighted grade.
evaluation:
  type: deterministic
  method: integration_test
  success_criteria: |
    the generated summary contains all listed fields for every executed atomic
    test and the final weighted grade, serialized as JSON
  failure_mode: emit critical warning and output partial summary
  guardrails:
    - summary size limit 10 MiB
    - JSON schema validated before persistence
  human_escalation: summary serialization raises an unhandled exception
